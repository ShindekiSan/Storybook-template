import { Story, Preview, Meta, Props } from "@storybook/addon-docs";
import { html } from "lit";

import '../elements/Button';

<Meta title="MDX/Button" component='custom-button' />

# Button

Button is the primary UI component for user interactions

export const Template = (args) => html`<custom-button variant=${args.variant} text=${args.text}></custom-button>`;

## Examples

### Contained

<Preview withToolbar>
  <Story
    name="Contained"
    args={{
      text: 'Click me!',
      variant: 'contained',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

### Outlined

<Preview withToolbar>
  <Story
    name="Outlined"
    args={{
      text: 'Click me!',
      variant: 'outlined',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

### Text

<Preview withToolbar>
  <Story
    name="Text"
    args={{
      text: 'Click me!',
      variant: 'text',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

## API

<Props of='custom-button' />